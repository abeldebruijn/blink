# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "blink"

[setup]
script = '''
#!/bin/bash
# Copy environment and config files
for file in .env .env.local .envrc .tool-versions; do
  if [ -f "$CODEX_SOURCE_TREE_PATH/$file" ]; then
    cp "$CODEX_SOURCE_TREE_PATH/$file" "./$file"
    echo "Copied $file"
  fi
done

pnpm i
'''
